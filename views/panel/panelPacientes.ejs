<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('../commons/head', { title: 'Mi Panel' }) %>
    <link rel="stylesheet" href="/public/css/panel/panel_pacientes.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>

    <div class="contenedor-flex">
        <div class="contenedor-barra-lateral">
             <%- include('../commons/barra-lateral-pacientes') %>
        </div>

        <div class="contenedor-panel-general">
            
            <div class="header-bienvenida">
                <div class="texto">
                    <h1>Hola, <%= usuario.nombre.split(' ')[0] %> 👋</h1>
                    <p>Bienvenido a tu portal de salud. Aquí tienes un resumen de tu actividad.</p>
                </div>
                <div class="boton-accion">
                    <a href="/panelPacientes/nuevo-turno" class="btn-nuevo-turno">
                        <i class="fa-solid fa-plus"></i> Solicitar Turno
                    </a>
                </div>
            </div>

            <% if (locals.proximoTurno) { %>
                <div class="tarjeta-proximo-turno">
                    <div class="icono-reloj">
                        <i class="fa-regular fa-clock"></i>
                    </div>
                    <div class="info-turno">
                        <h3>Próximo Turno Confirmado</h3>
                        <p class="fecha-grande">
                            <%= new Date(proximoTurno.fecha_hora).toLocaleDateString('es-AR', { weekday: 'long', day: 'numeric', month: 'long' }) %>
                            a las <%= new Date(proximoTurno.fecha_hora).toLocaleTimeString('es-AR', { hour: '2-digit', minute:'2-digit' }) %> hs
                        </p>
                        <p class="medico"><i class="fa-solid fa-user-doctor"></i> Dr/a. <%= proximoTurno.medico_nombre %></p>
                        <p class="ubicacion"><i class="fa-solid fa-location-dot"></i> Centro de Ojos Chivilcoy</p>
                    </div>
                    <div class="estado-badge confirmado">
                        Confirmado
                    </div>
                </div>
            <% } else { %>
                <div class="tarjeta-proximo-turno vacio">
                    <div class="info-turno">
                        <h3>No tienes turnos próximos</h3>
                        <p>¿Necesitas ver a un especialista?</p>
                    </div>
                    <a href="/panelPacientes/nuevo-turno" class="btn-link">Agendar ahora</a>
                </div>
            <% } %>

            <div class="grid-dashboard">
                
                <div class="card-dashboard">
                    <div class="card-header">
                        <h2><i class="fa-solid fa-prescription-bottle-medical"></i> Últimas Recetas</h2>
                        <a href="/panelPacientes/recetas" class="ver-todo">Ver todas</a>
                    </div>
                    <div class="card-body">
                        <% if (locals.ultimasRecetas && ultimasRecetas.length > 0) { %>
                            <ul class="lista-recetas">
                                <% ultimasRecetas.forEach(receta => { %>
                                    <li>
                                        <div class="info-receta">
                                            <span class="fecha"><%= new Date(receta.fecha).toLocaleDateString('es-AR') %></span>
                                            <strong><%= receta.tratamiento %></strong>
                                            <span class="doctor">Dr. <%= receta.medico_nombre %></span>
                                        </div>
                                        <div class="accion-receta">
                                            <% if (receta.archivo_firma_digital) { %>
                                                <a href="/public/recetas/<%= receta.archivo_firma_digital %>" target="_blank" class="btn-icon" title="Descargar PDF">
                                                    <i class="fa-solid fa-file-pdf"></i>
                                                </a>
                                            <% } else { %>
                                                <span class="badge-pendiente">En proceso</span>
                                            <% } %>
                                        </div>
                                    </li>
                                <% }) %>
                            </ul>
                        <% } else { %>
                            <div class="empty-state">
                                <i class="fa-solid fa-file-medical"></i>
                                <p>No hay recetas recientes.</p>
                            </div>
                        <% } %>
                    </div>
                </div>

                <div class="card-dashboard">
                    <div class="card-header">
                        <h2><i class="fa-solid fa-list-check"></i> Historial de Turnos</h2>
                        <a href="/panelPacientes/turnos" class="ver-todo">Ver todos</a>
                    </div>
                    <div class="card-body padding-0">
                        <% if (locals.historialTurnos && historialTurnos.length > 0) { %>
                            <div class="table-responsive">
                                <table class="tabla-resumen">
                                    <thead>
                                        <tr>
                                            <th>Fecha</th>
                                            <th>Médico</th>
                                            <th>Estado</th>
                                    </thead>
                                    <tbody>
                                        <% historialTurnos.forEach(turno => { %>
                                            <tr>
                                                <td>
                                                    <div style="font-weight: 600;">
                                                        <%= new Date(turno.fecha_hora).toLocaleDateString('es-AR', {day:'2-digit', month:'2-digit'}) %>
                                                    </div>
                                                    <small style="color: #888;">
                                                        <%= new Date(turno.fecha_hora).toLocaleTimeString('es-AR', {hour:'2-digit', minute:'2-digit'}) %> hs
                                                    </small>
                                                </td>
                                                
                                                <td><%= turno.medico_nombre %></td>
                                                
                                                <td>
                                                    <% 
                                                        let clase = 'pendiente';
                                                        if(turno.id_estado_turno == 3) clase = 'confirmado';
                                                        else if(turno.id_estado_turno == 4) clase = 'completado';
                                                        else if(turno.id_estado_turno == 5) clase = 'cancelado';
                                                    %>
                                                    <span class="badge-estado <%= clase %>">
                                                        <%= turno.estado_nombre %>
                                                    </span>
                                                </td>

                                            </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <div class="empty-state">
                                <i class="fa-regular fa-calendar-xmark"></i>
                                <p>No hay historial disponible.</p>
                            </div>
                        <% } %>
                    </div>
                </div>

            </div> </div>
    </div>

</body>
</html>